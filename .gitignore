# Godot 4.x
.godot/
/out/
export_presets.cfg.bak

# Imported asset cache + sidecars (regenerated on import)
*.import

# CC0 art assets — fetched via ./fetch_assets.sh (not committed; see README)
/models/
/skies/
/textures/

# Engine binary + export output + tooling
/godot
*.tmp
*.log

# Verification tooling (never part of the game)
/verify/

# OS / editor
.DS_Store
